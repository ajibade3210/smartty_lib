/**
 * @description
 * @param {number} n
 * @returns {boolean}
 * @example
 * isOdd(1) // true
 * isOdd(2) // false
 * */
// Add Comments
// Add Comments2

export const isOdd = (n: number) => n % 2 === 1;

export const isEven = (n: number) => n % 2 === 0;
