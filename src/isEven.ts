/**
 * @description
 * checks if argument recieved is an even number
 * @param {number} n
 * @returns {boolean}
 * @example
 * isEven(1) // false
 * isEven(2) // true
 * */

export const isEven = (n: number): boolean => n % 2 === 0;
