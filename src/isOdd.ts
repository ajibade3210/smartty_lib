/**
 * @description
 * checks if argument recieved is an odd number
 * @param {number} n
 * @returns {boolean}
 * @example
 * isOdd(1) // true
 * isOdd(2) // false
 * */

export const isOdd = (n: number): boolean => n % 2 === 1;
